<?xml version="1.0"?>
<application name="test the genration and the save of a matrix in a component">
<description> some tests on generation and save of a matrix
</description>
<graph>

compute XMP_genMat(mA,1,1);
compute XMP_saveInitMat(0,0,mA);

</graph>
</application>
